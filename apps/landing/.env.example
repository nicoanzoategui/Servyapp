# Número WhatsApp del CTA "Hablar con Servy" (solo dígitos con código país; wa.me)
NEXT_PUBLIC_WA_NUMBER=16206474920

# URL del backend Servy (CORS debe permitir el origen de esta app)
NEXT_PUBLIC_API_URL=http://localhost:3000

# URL pública del sitio principal (hogares) — usado en /tecnicos para enlaces "Soy cliente"
NEXT_PUBLIC_MAIN_ORIGIN=https://servy.lat

# Host del subdominio de técnicos (middleware reescribe / → /tecnicos). Opcional en local.
# NEXT_PUBLIC_TECNICOS_HOST=tecnicos.servy.lat
